# Public site key from your Turnstile widget (dash.cloudflare.com > Turnstile).
# Safe to expose in the browser bundle — this is baked in at build time.
VITE_TURNSTILE_SITE_KEY=0x4AAAAAAESKYhYHqA-BkLGS
